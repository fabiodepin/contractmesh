# Optional environment materialization manifest.
# Format:
# repo|template_relative_path|target_relative_path|renderer|VAR1,VAR2
#
# The bundled evaluation repositories do not require local env files.
